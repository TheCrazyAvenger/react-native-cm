import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    marginTop: 112,
    alignItems: 'center',
    marginBottom: 37,
    paddingHorizontal: 35,
  },
});
