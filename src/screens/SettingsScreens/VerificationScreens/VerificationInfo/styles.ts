import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  verified: {
    borderWidth: 1,
    marginVertical: 18,
    borderRadius: 48,
    paddingHorizontal: 12,
    paddingVertical: 4,
    alignItems: 'center',
  },
  text: {
    textAlign: 'center',
  },
});
