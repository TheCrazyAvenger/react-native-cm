import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontFamily: 'OpenSans-Regular',
    textAlign: 'center',
    marginTop: 25,
    marginBottom: 40,
  },
});
