import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    marginTop: 112,
    marginBottom: 35,
    marginHorizontal: 10,
    alignItems: 'center',
    paddingHorizontal: 35,
  },
});
