import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 0,
  },
  bodyContainer: {
    paddingTop: 20,
    paddingBottom: 4,
    paddingHorizontal: 20,
  },
});
