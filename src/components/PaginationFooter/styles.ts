import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    alignItems: 'center',
    paddingTop: 5,
    flex: 0.15,
    paddingHorizontal: 36,
  },
  nextButton: {
    marginTop: 20,
  },
});
