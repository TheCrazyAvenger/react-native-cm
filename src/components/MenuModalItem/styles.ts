import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 15,
  },
  image: {
    marginRight: 10,
    width: 30,
    height: 30,
  },
  title: {
    marginBottom: 2,
    fontFamily: 'OpenSans-Bold',
  },
});
