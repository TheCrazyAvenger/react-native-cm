import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  step: {
    height: 4,
    width: 30,
    marginHorizontal: 2.5,
    borderRadius: 2,
  },
});
